class OpenAccessProject {
  constructor(systemID, systemName, teamName, contact) {
    this.systemID = systemID
    this.systemName = systemName
    this.teamName = teamName
    this.contact = contact
  }
}

export default OpenAccessProject
